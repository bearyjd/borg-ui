shared/*.exe
shared/*.msi
shared/smoke-results.json
shared/MicrosoftEdgeWebview2Setup.exe
results.log
validate.log
validate-edge.log
# Downloaded installer artifacts for validate-installer (INSTALLER_DIR default)
installers/
.bin/
# Any local installer download dir, not just the INSTALLER_DIR default above —
# `gh run download` into a new name must not commit ~50 MB of binaries.
installers-*/
